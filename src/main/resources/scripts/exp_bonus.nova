// 经验加成属性脚本
// 触发类型: KILL
// 参数: attacker, victim, ctx, attrValue (额外经验值)

fun execute(attacker, victim, ctx, attrValue) {
    if (attrValue > 0 && isPlayer(attacker)) {
        giveExp(attacker, attrValue)
    }
}
