// 凋零属性脚本
// 触发类型: ATTACK
// 参数: attacker, victim, ctx, attrValue (凋零几率)

fun execute(attacker, victim, ctx, attrValue) {
    if (chance(attrValue)) {
        applyEffect(victim, "WITHER", 60, 0)
    }
}
