// 减速属性脚本
// 触发类型: ATTACK
// 参数: attacker, victim, ctx, attrValue (减速几率)

fun execute(attacker, victim, ctx, attrValue) {
    if (chance(attrValue)) {
        applyEffect(victim, "SLOW", 40, 0)
    }
}
